OPENAI_API_KEY=sk-...

# Required for birdcall/ingest/xenocanto.py — Xeno-canto's v3 API needs a free key.
# Register at xeno-canto.org with a verified email, then find your key on your account page.
XENO_CANTO_API_KEY=...

# Optional: swap birdcall/reasoning/llm_reasoning.py's _call_llm() to use this instead
# (same request/response shape) if you'd rather run the reasoning layer on Claude.
# ANTHROPIC_API_KEY=sk-ant-...
